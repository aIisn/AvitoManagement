# Minimal requirements for AvitoManagement project
# Core web framework
Flask==3.0.0
Flask-Cors==4.0.0

# Image processing
Pillow==10.2.0
numpy==2.2.6

# Google Sheets integration
gspread==6.2.1
oauth2client==4.1.3
google-auth==2.27.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# Security and authentication
bcrypt==4.1.2
cryptography==46.0.2
PyJWT==2.10.1

# Timezone handling
pytz==2025.2

# Redis caching and session storage
redis==6.4.0

# HTTP requests
requests==2.32.5

# Core dependencies (automatically installed with above)
# Werkzeug, Jinja2, MarkupSafe, click, itsdangerous, blinker
# httplib2, oauthlib, requests-oauthlib, pyasn1, pyasn1-modules
# cachetools, certifi, cffi, charset-normalizer, idna, pycparser
# pyparsing, rsa, six, urllib3
